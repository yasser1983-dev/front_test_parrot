import orderReducer, { addOrder } from './orderSlice';

// test('should add an order', () => {
//     const initialState = { orders: [] };
//     const newOrder = { id: 1, product: 'Pizza' };
//     const result = orderReducer(initialState, addOrder(newOrder));
//     expect(result.orders).toContainEqual(newOrder);
// });